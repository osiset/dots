;; Network
[module/check-network]
type = custom/script
exec = ~/.config/polybar/scripts/check-network
tail = true
click-left = /usr/bin/urxvt -e sh -c "/usr/bin/nmtui"

;; Updates
[module/check-updates]
type = custom/script
exec = ~/.config/polybar/scripts/check-updates
tail = true
interval = 5
; Available tags:
;   <output> - deprecated
;   <label> (default)
;;format = <label>
;;format-background = #999
;;format-foreground = #000
;;format-padding = 4
; Available tokens:
;   %output%
; Default: %output%
;;label = %output:0:15:...%
; Available tokens:
;   %counter%
;   %pid%
;
; "click-(left|middle|right)" will be executed using "/usr/bin/env sh -c [command]"
; Available tokens:
;   %counter%
;   %pid%
;
; "scroll-(up|down)" will be executed using "/usr/bin/env sh -c [command]"
;;scroll-up = echo scroll up %counter%
;;scroll-down = echo scroll down %counter%

;; Spotify
[module/spotify]
type = custom/script
exec = ~/.config/polybar/scripts/spotify -f '{artist}: {song}'
interval = 1
format-prefix = " "
format = <label>
format-underline = "#1db954"

;; Bluetooth
[module/bluetooth]
type = custom/script
interval = 3
exec = ~/.config/polybar/scripts/bluetooth
click-left = /usr/bin/blueberry

;; Notification History
[module/notifications]
type = custom/script
interval = 3600
exec = ~/.config/polybar/scripts/notifications
click-left = "/usr/bin/xdotool keydown Control Shift grave; sleep 0.1; /usr/bin/xdotool keyup Control Shift grave;"
